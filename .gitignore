node_modules/
dist/
*.log
.DS_Store
.env
*.tgz

# Yarn 4 — keep releases/plugins/patches/sdks/versions but ignore everything else.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# Local commit-message scratch file (PowerShell here-string workaround)
.git-commit-msg.tmp

# Stray nested clone (can't be removed cleanly — ignore it)
companion-module-waves-lv1/
